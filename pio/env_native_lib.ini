[env:native_lib]
platform = native
framework =
lib_ldf_mode = chain+
# Build the library sources without running tests
test_build_src = no

build_unflags =
    ${common.build_unflags}

build_flags =
    ${common.build_flags}

lib_deps =
    ${common.lib_deps}
